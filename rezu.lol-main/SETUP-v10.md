# v10 quick polish

This update is code-only. No new SQL is required.

Fixes:
- Enter screen text can now be fully deleted or changed without snapping back to `click to enter`.
- Discord presence cards are centered consistently under profile layouts.
- Audio shuffle ignores empty/broken tracks and rotates through real uploaded tracks.
- Dashboard audio manager ignores old empty track records.
- Added a `typewriter` username effect.

After replacing the project, redeploy Vercel.
